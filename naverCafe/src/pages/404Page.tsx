const NotDefinedPage = () => {
  const style = {
    display: "flex",
    justifyContent: "center",
    alignItems: "center",
    height: "600px",
    fontWeight: "bold",
  };
  return <div style={style}>없는 페이지입니다.</div>;
};

export default NotDefinedPage;
